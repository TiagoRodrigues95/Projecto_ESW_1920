@model IEnumerable<CandidaturaE_.Models.MarcacaoDuvidas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HoraId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UtilizadorId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoraId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UtilizadorId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MRId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MRId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MRId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
